// ✅ CORRECT
{
  "name": "flipzone-e-store",
  "version": "1.0.0",
  "scripts": {
    "build": "cd Back-end && npm install && cd ../Front-end && npm install && npm run build",
    "start": "cd Back-end && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}